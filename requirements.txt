# Core dependencies
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
sqlmodel>=0.0.8
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# TTS and audio
edge-tts==7.0.2
mutagen>=1.45.0

# Streaming platforms
# Note: TwitchIO 3.x requires additional parameters (client_id, client_secret, bot_id)
# The code is compatible with both 2.x and 3.x, but 2.x is more stable
twitchio>=2.0.0,<4.0.0
aiohttp>=3.8.0

# Image processing
Pillow>=9.0.0

# Cloud services
boto3>=1.26.0

# Google APIs (YouTube)
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-api-python-client>=2.100.0

# Testing dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.24.0